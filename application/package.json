{
  "name": "application",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "prepublish": "typings install && gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@company/core-package": "*"
  },
  "devDependencies": {
    "typescript": "^1.8.7",
    "typings": "^0.7.9"
  }
}
